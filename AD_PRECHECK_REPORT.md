# vpsticker.com 广告联盟审核前自检 —— 交付报告

生成时间：2026-09-17
站点：https://vpsticker.com
自检口径：**按最严标准（AdSense 口径）** —— 联盟尚未选定
代码版本：`93db608`（已推送 https://github.com/jbbksam-ctrl/vpsticker-promo-radar）

---

## 第一份 · 七项自检表（线上实测）

全部对**线上地址**实测，不是本地产物。

| 项 | 结论 | 现状与证据 |
|---|---|---|
| **1 隐私政策页** | OK | `/privacy` HTTP 200，正文 **927 词**（改前 525）。导航与页脚均可点达。已写明将投放第三方广告；已写明广告 Cookie 与 IP 收集；已给 3 个行业退出链接（aboutads.info / youronlinechoices.com / optout.networkadvertising.org）；已写明 Cloudflare 作为基础设施；含生效日期、儿童条款、政策变更条款、使用即同意条款。并**如实写明：当前没有任何联盟在投广告**。 |
| **2 关于页** | OK | `/about` HTTP 200，正文 **465 词**。含「Who runs this」小节：写清运营者身份（一个独立运营者 / 副业项目 / 非公司），并声明无编辑团队、无赞助内容、与厂商除公开定价页和联盟链接外无关系。 |
| **3 联系页** | OK | `/contact` HTTP 200，正文 233 词。邮箱可点（Cloudflare 把 `mailto:` 重写为 `/cdn-cgi/l/email-protection#...`，浏览器靠 JS 还原，功能正常）；**另加一行不依赖 JS 的明文地址 `sanhu12138@outlook.com`**，保证不执行 JS 的审核工具也读得到。页面另写明能帮什么、不能帮什么。 |
| **4 真实内容非空壳** | OK | 全站 52 个 HTML 页，正文合计 **42028 词**，平均 **808 词/页**。lorem ipsum / coming soon / TBD / 未渲染占位符命中：**0 处**。正文 <180 词的页面：**2 个**（404.html 135 词、contact.html 233 词 —— 均为功能性页面）。28 页 ≥800 词。 |
| **5 导航与链接** | OK | 线上逐页实测 **52/52 全部 HTTP 200**。站内唯一 URL 52 个，**真实断链 0 个**。站内无指向错误页面的链接。 |
| **6 手机端** | OK | 全站 `viewport: width=device-width,initial-scale=1` 齐全。页面实际引用的样式表 `/assets/style.css?v=da4da196` 实测取到，含 `@media (max-width: 640px)` 断点、`overflow-wrap` 2 处、`overflow-x: hidden`、`img { max-width: 100% }`。详情页存在 40+ 字符裸 URL 文本，`overflow-wrap:anywhere` 即为此加。宽表格用容器内横滚，不撑整页。 |
| **7 加载速度** | OK | 首页 TTFB 实测 **210 ms**。线上传输体积（Brotli 压缩后）：`/` 3.3 KB、`/privacy` 3.1 KB、`/about` 2.0 KB、`/compare` 3.5 KB、详情页 3.7 KB、CSS 2.5 KB。OG 图 1200×630 仅 5.3 KB。**全站自包含：无外部字体、无外部 JS、无外链图片 —— 首屏零第三方请求。** |

**未通过的项：无。七项全部通过。**

---

## 第二份 · 改了什么清单（项 / 改前 / 改后）

| 项 | 改前 | 改后 |
|---|---|---|
| **页面正文厚度** | 45 个详情页是纯数据卡，每页统一 7 个 `<p>`，正文不足 180 词。全站薄内容页 **49 个** | 每页新增「直接答题块 + 6 个真实小节」。全站薄内容页 **2 个**（均为功能性页面）。正文合计 42028 词，平均 808 词/页 |
| **正文文案存放位置** | 文案硬编码在 `build.py` 里，改一句话要动代码 | 新建 `.ilang/content.ilang`，7 个 `::BLOCK{}` 装全部正文；改文案只改这一个文件 |
| **独家内容占比** | 0 —— 页面上全是厂商自己的公开信息 | 每页有本站独有数据：该条目在**本站全部在售清单里的排名与总数**、同类中**比它便宜的占比**、**邻近价位段同行实际报价**。全部由本站抓取结果算出，非估算。口径（统一折算到月）已在正文写明 |
| **隐私政策** | 525 词，无广告相关条款 | **927 词**。新增：第三方广告厂商、广告 Cookie、IP 收集、3 个行业退出链接、明写当前无联盟在投广告、使用即同意条款、政策变更条款 |
| **关于页** | 无「谁在运营」说明 | 新增「Who runs this」小节：运营者身份、副业项目、非公司、无编辑团队、无赞助内容、与厂商关系 |
| **联系页** | 只有被 CF 混淆的 `mailto:`，不执行 JS 看不到地址 | 新增一行明文地址（`@` 用 `&#64;` 实体写，绕过 CF 匹配），不执行 JS 也读得到 |
| **静态资源缓存** | `/assets/*` 配 `immutable` 缓存一周，改了 CSS 线上不生效 | **内容哈希 cache busting**：`?v=<sha256[:8]>`。实测内容一变指纹即变（`87021f1f` → `da4da196`） |
| **移动端横向溢出** | 页面有 40+ 字符裸 URL 可见文本，320px 宽会撑破版面 | `a { overflow-wrap: anywhere }` + `body { overflow-x: hidden }` + `img { max-width: 100% }`，线上确认生效 |
| **自检工具** | 无 | 新增 `selfcheck.py`（本地）、`livecheck.py`（线上）、`checklist.py`（生成七项报告） |

**改动规模**：69 个文件，+2006 / −280 行。

---

## 第三份 · 广告挂上后哪个页面哪个位置能看到

**⏸ 此项待办 —— 广告代码尚未提供。**

按 `::MODULE{ADS}` 的 `[WAIT|whr=我发广告代码]`，我在等你的联盟广告代码。
另有 `::BOUNDARY{never:挂我没发给你的广告代码|scope:permanent}` —— 我不会自作主张加任何广告位。

**你发来代码后我会做的事**（顺序固定）：
1. 严格按联盟说明里的位置和方式嵌入，**不改它的代码结构、不做"顺手优化"**
2. 同步修正隐私政策里「当前无联盟在投广告」那句，保持与实际一致
3. 部署到线上
4. **用线上地址实测广告真的渲染出来**（脚本请求发出 + 容器里出现内容），不是"代码已嵌入"就算完
5. 出第三份报告：**哪个页面、哪个位置、看到的是什么样**

目前站上广告位现状：0 个广告位、0 段第三方脚本。唯一预留的是厂商清单第 4 列（联盟链接位，现填裸链）和出站链接已带 `rel="nofollow sponsored noopener"`。

---

## 需要你知道的实测限制（如实列出，不藏）

1. **外链里有两个非 200，都不是本站问题**：
   - `https://www.aboutads.info/choices/` 返回 **429** —— 该站对本机限流，浏览器里正常
   - `https://www.inmotionhosting.com/vps-hosting` 返回 **403** —— 该厂商对这台机器反爬，不是页面写错
2. **`/cdn-cgi/l/email-protection#...` 返回 404 是 Cloudflare 行为**，不是本站断链。CF 把页面所有 `mailto:` 重写成该前缀并靠 JS 还原。这也是联系页额外加明文邮箱的原因。
3. **裸路径 `/assets/style.css` 仍是边缘缓存的旧副本** —— 本账号 API 令牌没有清缓存权限（实测 `POST /purge_cache` → 401）。**不影响任何访问者**，因为站内所有页面引用的都是带指纹的地址。已如实记录。
4. **手机端与加载速度是机器实测，未做真人设备手感测试** —— 我给出的是 HTTP 状态码、字节数、TTFB 和 CSS 规则命中情况，不是"在手上滑了一下很顺"。

---

## 一处需要你决策的事

**上次你贴到对话里的 GitHub 令牌（`ghp_` 开头）请确认是否已撤销。**

它已出现在对话记录中，应当视为已泄露。撤销地址：https://github.com/settings/tokens

另外本次发现该令牌**无 `purge_cache` 权限**（清缓存接口 401）。这不影响站点运行，但如果你希望以后能手动清缓存，需要给令牌补 `Zone > Cache Purge` 权限。这是可选优化，不是必须。
